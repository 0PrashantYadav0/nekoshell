## What this changes

<!-- One or two sentences. What a user or contributor gets that they did not have. -->

## How it was verified

<!-- `make check` output, the terminals it was tried in, a screenshot if the look changed. -->

## Checklist

- [ ] `make check` passes (lint and the bats suite)
- [ ] Every commit message is a conventional commit (`feat:`, `fix:`, `docs:`, ...) and is signed off (`git commit -s`)
- [ ] New behaviour has a test under `tests/`
- [ ] Docs that describe the changed behaviour are updated (README, docs/, the plugin or terminal README)
- [ ] `CHANGELOG.md` has a line under the unreleased version, if a user would notice the change
