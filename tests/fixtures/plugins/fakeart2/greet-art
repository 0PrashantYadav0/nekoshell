#!/usr/bin/env bash
# Fixture provider: a caption and three lines, or nothing at all when
# FAKEART2_FAIL=1, the way a provider whose pack never arrived behaves.
[[ "${FAKEART2_FAIL:-}" == 1 ]] && exit 1
echo "Fake Art · two"
printf '▄▄▄\n███\n▀▀▀\n'
