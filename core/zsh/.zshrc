# nekoshell zshrc. This is a placeholder: task A5 fills it in with the real
# env.zsh -> theme.zsh -> antidote -> aliases.zsh -> plugin bin/plugin.zsh ->
# starship -> plugin late.zsh -> local.zsh load order. Its only job for now is
# to exist at a stable path so `nekoshell doctor` can tell a real nekoshell
# symlink apart from a stray file.
